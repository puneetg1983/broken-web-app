@{
    ViewData["Title"] = "Out of Memory Scenario 1 - Running";
}
<div class="container">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card">
                <div class="card-header bg-dark text-white">
                    <h2>Out of Memory Scenario 1 - Running</h2>
                </div>
                <div class="card-body">
                    <div class="alert alert-dark">
                        <strong>Out of Memory in Progress...</strong>
                    </div>
                    <div class="text-center">
                        <div class="spinner-border text-dark" role="status">
                            <span class="visually-hidden">Loading...</span>
                        </div>
                        <p class="mt-3">The application is simulating an out of memory condition by allocating large objects...</p>
                    </div>
                    <div class="mt-4">
                        <a href="@Url.Action("Index", "Home")" class="btn btn-secondary">
                            <i class="fas fa-arrow-left"></i> Back to Scenarios
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div> 